# NodeChat

Next.js app for secure messaging.

- `frontend/` – Chat service as a Next.js app.
- `ws-server/` – Standalone WebSocket server for real-time messaging.

Required environment variables are listed in each server's directory.
